# 问题与需求登记

> 用途：在日常使用系统过程中，随手登记遇到的问题（Bug）和想优化的点（需求/改进）。
> 用法：直接在下方表格新增一行即可，先记下来不用想太细，后续再整理分级。

---

## 使用说明

- **快速登记**：遇到问题先填「编号 / 日期 / 类型 / 模块 / 标题 / 描述」即可，其余字段可留空，事后补。
- **编号规则**：`类型缩写-序号`，如 `B-001`（Bug）、`F-001`（功能/需求）、`O-001`（优化）。
- **类型**：`Bug` 缺陷 / `需求` 新功能 / `优化` 体验或性能改进。
- **优先级**：`P0` 阻塞必须立刻处理 / `P1` 重要尽快 / `P2` 一般 / `P3` 有空再说。
- **状态**：`待处理` / `处理中` / `已完成` / `已搁置` / `已取消`。
- **模块**：如 `数据准备`、`数据回测`、`CNN`、`实盘交易`、`前端`、`后端`、`其他`。

---

## 登记表

| 编号 | 日期 | 类型 | 优先级 | 模块 | 标题 | 详细描述 | 期望结果 | 状态 | 备注 |
|------|------|------|--------|------|------|----------|----------|------|------|
| B-001 | 2026-06-07 | Bug | P2 | 示例 | 这里写一句话标题 | 复现步骤 / 现象 / 报错信息 | 期望它怎样表现 | 待处理 | 截图、日志链接等 |
| B-002 | 2026-06-09 | Bug | P2 | 后端/测试 | upload 集成测试取价缺失 | `tests/test_live_upload_integration.py::test_upload_branch_end_to_end_reads_imported_close` 失败：CSV 导入后 `_load_close_price` 报「行情缺失」 | CSV 导入后取价链路命中导入收盘价 | 已完成 | 根因：测试用默认 `save_mode=batch`（仅暂存待合并批次），未写正式资源；改为 `save_mode=official` 后 3 例全过 |
| F-010 | 2026-06-09 | 需求 | P2 | 实盘交易 | 交易计划自动化残余缺口 | 自动触发使用「保存计划时的组合快照」（快照滞后）；降级 `health_fn` 当前为占位恒健康；调度仅按交易日历，未接节假日历；pull 数据就绪依赖手动验证 | 后续接入实时组合快照、真实健康探针、节假日历与 pull 自动校验 | 待处理 | 见 `.kiro/specs/trading-plan-automation/design.md` 已知缺口 |
| F-011 | 2026-06-09 | 需求 | P2 | 实盘交易 | 盘中监控决策（Phase 2） | **已落地**：日内 `bar_freq`（1m/5m/10m/15m/30m/60m）与模型 `input_interval` 间隔锁定；`bar_close_grid` + `due_bar_slot` 监控模式（交易时段每根 bar 收盘后自动决策，数据滞后自动重试）；warm-up 按 bar 数换算；前端 PlanForm/ConfigForm/PlanList 适配。**余项（Wave C）**：盘中实时行情自动拉取、降采样、重试退避策略 | 开盘时段按模型训练间隔持续监控并决策，无前视 | 已完成 | 见 `.kiro/specs/intraday-monitoring-decision/`；回测同频复现仍用回测引擎 |
| B-003 | 2026-06-09 | Bug | P3 | 前端/测试 | DataPrepare/CNNTrain 测试全量并发失败 | `DataPrepare.batches.test.tsx` 断言已废弃按钮名「预检并合并」（组件已改名「合并到正式K线」）；`CNNTrain.test.tsx` 重型用例在全量并发 CPU 争用下整测超时 | 测试与组件对齐 + 放宽超时 | 已完成 | 改测试按钮名为「合并到正式K线」；setup 加 RTL asyncUtilTimeout=5s、vitest testTimeout→30s；前端 173 passed 全绿 |
| F-012 | 2026-06-12 | 需求 | P2 | 后端/前端 | 任务与调度可观测性（TSO） | **已落地**：任务执行记录（耗时/堆栈/参数脱敏）、任务历史 JSONL 持久化（重启可查、API 过滤合并）、调度运行日志（六类跳过原因+当日去重，回答"为什么没跑"）、trigger_source/elapsed_ms/notify_ok 贯穿、通知发送实录、GET /scheduler/runs、前端调度日志卡/last_triggered/任务耗时与 traceback 折叠、统一 logging+可选轮转+dev.sh 超限归档 | 重启不丢任务历史、调度可排障、通知实测可见 | 已完成 | 见 `.kiro/specs/task-scheduler-observability/`（TSO-1~8 属性测试守护）；手动项：跑一次 `./dev.sh restart` 验证 backend.log 超限归档 |
| O-001 | 2026-06-12 | 优化 | P3 | 后端 | TSO 残余缺口 | alpha 包 loguru 与 stdlib logging 未桥接（alpha 日志仍 stdout-only 不进轮转文件）；JsonlDayStore 跨进程去重不保证；任务历史/调度日志无自动清理；调度历史无聚合分析视图（如近 30 天触发成功率）；ws 推送仍是死管道 | v2 逐项补齐 | 待处理 | 见 `.kiro/specs/task-scheduler-observability/design.md` 已知缺口节 |
| O-002 | 2026-06-12 | 优化 | P2 | CNN | path_class v1 残余：实盘 SignalService 否决未接线 | SignalService 按 signal=prob_tp 标量消费语义在实盘侧成立；veto_threshold 否决逻辑目前仅回测 CNNSignalStrategy 可用，实盘信号推送流程未读取 prob_sl 做实时拦截 | 实盘侧接入 veto：在 SignalService 发送决策前检查 prob_sl >= veto_threshold 并标记为否决 | 待处理 | cnn-path-multiclass-head v1 已知缺口；不阻塞回测与训练使用 |
| O-003 | 2026-06-12 | 优化 | P3 | CNN | path_class 类别不平衡未自动处理 | build_dataset 已透出 class_distribution 供用户查看，但训练时不做自动加权或重采样；障碍宽度选不当（如 take_profit/stop_loss 过小）时 time_up/time_down 类占绝对多数，模型易退化为预测 time 类 | 提供类别权重选项（如 class_weight="balanced"）或自动重采样策略 | 待处理 | cnn-path-multiclass-head v1 已知缺口；不阻塞，用户可通过调整障碍宽度缓解 |
| O-004 | 2026-06-12 | 优化 | P3 | CNN | OCO 障碍宽度无波动率自适应 | take_profit/stop_loss 采用固定比例（如 3%）；三重障碍法标准做法是按滚动 ATR/波动率缩放障碍宽度，使不同市场环境下预期持有时长趋于一致 | 支持 take_profit/stop_loss 按波动率自适应（如 ATR 倍数模式） | 待处理 | cnn-path-multiclass-head v1 已知缺口；不阻塞，固定比例在平稳市况下足够 |
| B-004 | 2026-06-13 | Bug | P1 | CNN | 种子假多种子——治理多种子训出同一模型 | `_train_governance_model` 以 `seed_index`（0/1/2…）直接传给 `train_cnn_model`，而后者内部写死 `torch.manual_seed(42)`，导致不同 `seed_index` 实际使用同一随机状态；`run_walk_forward_evaluate` 的 `n_seeds` 循环也从未下传不同 seed；稳健性验证形同虚设，多种子 std 恒为 0 | 治理多种子循环映射真实 seed（BASE_SEED + seed_index），WF 汇报真实的跨种子均值/std | 已修复 | 根因：seed 参数缺失+硬编码；修复：`train_cnn_model` 加 `seed: int = 42` 参数，`_train_governance_model` 映射 `seed=BASE_SEED(42)+seed_index`，WF 内折套 n_seeds 循环聚合 cross_seed={mean,std,n}；见 `.kiro/specs/cnn-eval-honesty-fixes/`，分支 `feat/cnn-eval-honesty-fixes`，Property 1/2/3 守护 |
| B-005 | 2026-06-13 | Bug | P1 | CNN | 阈值语义漂移——回归模型静默套用概率阈值 | `predict_cnn_signals` 返回的信号帧不携带 `objective` 元信息；回测 API 端点、策略 `on_init`、实盘 `SignalService` 均无法区分当前阈值是概率型（0~1）还是收益型（可任意大小）；regression 模型配 `buy_threshold=0.6`（概率默认值）静默不交易，无任何报错 | 信号帧携带 `objective` 常量列；三处消费方接入 `threshold_scale_check` 校验，语义不匹配时提前拦截 | 已修复 | 修复：`predict_cnn_signals` 末列写入常量 `objective`；新增 `cnn/thresholds.py:threshold_scale_check()`；回测端点（400）、策略 `on_init`（_threshold_invalid 防御）、SignalService（违规返 hold）三处接入；见 `.kiro/specs/cnn-eval-honesty-fixes/`，Property 4/5 守护 |
| B-006 | 2026-06-13 | Bug | P2 | CNN | 对齐内连接静默丢样本——停牌致系统性样本偏差无告警 | `_align_frames_by_datetime` 使用 inner join 对齐多标的时间轴；观测组中任一标的停牌，对应时间步整体丢弃；停牌比例高时（如节假日、长期停牌龙头）大量样本被静默删除，训练集出现系统性偏差，但训练结果不报告丢失比例 | 对齐后透出丢弃率；超阈值时写警告到 result/on_meta，不静默 | 已修复 | 修复：`features.alignment_drop_rate(symbol_frames, aligned_height)`（max 基准，单标的=0）；训练 info 写 `alignment_drop_rate`/`alignment_warning`，推理 `on_meta` 写 `alignment_drop_rate`，trainer 写 checkpoint/result warnings；常量 `ALIGN_DROP_WARN_THRESHOLD=0.05`；见 `.kiro/specs/cnn-eval-honesty-fixes/`，Property 6 守护 |
| O-005 | 2026-06-13 | 优化 | P3 | CNN | 迭代 0 诚实性修复残余缺口 | ① GPU 非确定性：seed 在 CPU 完全可复现，GPU 未强制 `cudnn.deterministic`（性能权衡），单种子复现可能微小浮动；② regression 阈值校验是启发式：`buy≥0.5` 拦概率默认值误用，拦不住"刻意设 0.4 超大收益阈值"等罕见误配；③ 前端跨种子未接线：`n_seeds` 后端可用但前端 baseRequest 不发送（UI 触发不了多种子）、治理页未展示 `avg_cross_seed_std`；④ 回测请求重复 torch.load checkpoint：端点预检读 objective 一次 + `_run_cnn_backtest` 再读一次，同一请求双重反序列化权重（latency follow-up，非正确性问题）；⑤ WF candidate checkpoint N× 堆积：治理 WF 每折训 `n_seeds` 个模型，落 `folds×n_seeds` 个 .pt 无清理（既有清理缺口被放大，n_seeds≤10 时最多 10×） | 按优先级逐项补齐（③ 前端可视化影响用户直接使用，建议优先） | 待处理 | 不阻塞核心训练/推理/回测使用；见 `.kiro/specs/cnn-eval-honesty-fixes/` 残余缺口节 |
| O-006 | 2026-06-15 | 优化 | P3 | CNN选股 | G-1：实盘选股过滤未接线 | 选股榜单仅服务训练前选型，未接入实盘 SignalService 做入场二次过滤；实盘侧无法复用选股漏斗的 Tier-1/Tier-2 结论做动态股池管理 | 在 SignalService 或调度层接入 ScreeningResult 做二次过滤，结论 draft 态自动降级为候选 | 待处理 | CNN选股 v1 已知缺口 G-1；不阻塞选股与训练使用；见 `.kiro/specs/cnn-stock-screening/design.md` |
| O-007 | 2026-06-15 | 优化 | P3 | CNN选股 | G-2：绝对 edge 为启发式阈值、无 buy&hold 超额对照 | `edge_ok` 采用「平均 candidate_score>0 + 正折比≥阈值」启发式判据，非统计显著性检验；WF 路径未提供 buy&hold 对照，无法派生相对超额（`run_walk_forward_evaluate` 仅有 `candidate_statistics.total_return`，逐折对比 buy&hold 需额外计算） | 引入相对超额门禁（逐折对比 buy&hold）或统计显著性检验（如 t-test on fold scores）增强 edge 判据 | 待处理 | CNN选股 v1 已知缺口 G-2；不阻塞；见 `.kiro/specs/cnn-stock-screening/design.md` |
| O-008 | 2026-06-15 | 优化 | P3 | CNN选股 | G-4：Tier-1 大池串行成本 | universe 很大（如全 A 股）且用分钟级周期时，逐只串行画像偏慢；v1 靠 `min_bar_count`/`exchange` 收窄和 `top_k` 控制，但极端情况下仍可能超时 | Tier-1 逐只画像并行化（进程池或协程），增设超时熔断，减少大池运行时间 | 待处理 | CNN选股 v1 已知缺口 G-4；不阻塞；见 `.kiro/specs/cnn-stock-screening/design.md` |
| O-009 | 2026-06-15 | 优化 | P3 | CNN选股 | G-5：ScreeningRules 默认权重未跨市场标定 | 内置权重（volatility/liquidity/nonlinearity/pattern_recurrence/temporal_stability 各维）为经验默认，未在 A/H/US 等不同市场与周期上系统标定；跨市场使用时可能偏优或偏劣 | 对多市场历史回测标定权重，或提供权重自动校准（如 WF 内梯度搜索/网格搜索）；已配置化，用户可手动覆盖 | 待处理 | CNN选股 v1 已知缺口 G-5；不阻塞；见 `.kiro/specs/cnn-stock-screening/design.md` |
| F-013 | 2026-06-15 | 需求 | P3 | CNN选股 | G-6：多观测组输入与对齐丢弃率告警（v1 不做） | v1 Tier-2 为单标的，`CNNScreeningRequest` 不含 `observation_groups`；若未来 Tier-2 接入多观测组输入，须复用 `cnn-eval-honesty-fixes` 的 `alignment_drop_rate` 告警机制，否则静默丢弃样本 | Tier-2 支持 `observation_groups` 字段；接入 `features.alignment_drop_rate` 超阈值告警；与 honesty-fixes 合并迭代 | 待处理 | CNN选股 v1 已知缺口 G-6；Requirement 9.4 在 v1 不被触发；见 `.kiro/specs/cnn-stock-screening/design.md` |
| O-010 | 2026-06-15 | 优化 | P3 | CNN选股 | G-7：Tier-2 瞬态折模型写入 CNN_MODEL_DIR 未隔离 | `run_walk_forward_evaluate` 的注入 `store` 只路由 reports/history/production，逐折训练的临时 `.pt` 仍写入全局 `CNN_MODEL_DIR`；Tier-2 跑多只标的时堆积 `folds×n_seeds` 个瞬态模型，与生产模型目录混杂 | 在训练函数加可选模型目录参数，让 screening Tier-2 写入 `SCREENING_GOVERNANCE_PATH/models/`；需第二个侵入点 | 待处理 | CNN选股 v1 已知缺口 G-7；审计轨迹（reports/history/production）已隔离，瞬态模型是既有 WF 行为；见 `.kiro/specs/cnn-stock-screening/design.md` |
| O-011 | 2026-06-15 | 优化 | P2 | CNN选股 | Tier-2 薄数据体验：数据不足预检 + 窗口夹取 + 可配置 | 薄数据标的（如仅约 1 年 30m 数据）跑 Tier-2 时，默认 `eval_window_days=900` 让 `start` 落到数据存在之前，governance 第一折训练窗无数据抛 load error，runner 降级为晦涩的「Tier-2 失败: 无可用 30m 数据」 | 窗口左界夹进本地数据范围（`start=max(desired_start, local_start)`）；数据不足时清晰预检跳过（「数据不足：本地可用 X 天 < 最少需 Y 天」），不调用 WF；Tier-2 窗口/训练超参支持请求级覆盖并回显 | 部分落地/已处理 | 已落地：`_resolve_tier2_window` 统一解析+夹取、数据充足性预检、`run()` 配置不变量快速失败、`CNNScreeningRequest` 新增 eval_window_days/train_days/fold_test_days/n_seeds 覆盖、`input.tier2_config` 回显；测试 `tests/screening/test_tier2_config.py`（10 例，含用户场景回归）。余项：前端「高级设置」暴露这些覆盖项 |
| F-014 | 2026-06-19 | 需求 | P2 | 数据准备 | 数据准备页支持上传 parquet（文件夹批量多支股票） | **已落地**：K线/Tick 上传区同时接受 .csv 与 .parquet 并支持多选/文件夹；走「流式暂存→轻量预览汇总→确认→异步逐文件入待合并批次」；按文件名识别代码（裸码补交易所）、文件内 symbol 列则按列分组；复用既有批次合并管线；session_id 与代码均做路径穿越校验、只写 imports 层。**残余缺口**：单个超大 parquet 内部进度仅文件级（不细分）；一次 multipart 长请求仅浏览器整体上传百分比、无逐文件上传进度；datetime 为整数 epoch 时按毫秒解释（秒/纳秒级可能偏差，预览 date_range 会暴露异常）；同一 session_id 并发触发 import 后端未加锁（前端已防重复点击） | 后续按需补：单文件内部进度、逐文件上传进度、epoch 单位自适应、同会话导入幂等锁 | 已完成 | 见 `.kiro/specs/data-prepare-parquet-upload/`；后端属性测试 Property 1-8 + 端点/安全用例守护；均不阻塞 v1 |
|  |  |  |  |  |  |  |  |  |  |

---

## 详细记录区（可选）

> 表格里一行写不下的复杂问题，在这里展开。用 `### 编号 标题` 起一节，方便从表格跳转。

### B-001 示例标题

- **复现步骤**：
  1. 
  2. 
- **实际结果**：
- **期望结果**：
- **相关文件/日志**：
- **排查记录**：
